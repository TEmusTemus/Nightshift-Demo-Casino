# Required in production: use 32+ random bytes, never commit the real value.
SESSION_SECRET=replace-with-a-long-random-secret

# Local development uses db/casino.db. Production must use a durable PostgreSQL URL.
DATABASE_URL=postgres://user:password@host:5432/nightshift
